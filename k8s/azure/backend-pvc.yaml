apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: backend-azurefile-claim
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: azurefile
  resources:
    requests:
      storage: 5Gi
